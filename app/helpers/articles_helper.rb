module ArticlesHelper
  # def user_is_authorized_for_article?(article)
  #   current_user && (current_user == article.user)
  #   # current_user && (current_user == article.user || current_user.admin?)
  # end
end
